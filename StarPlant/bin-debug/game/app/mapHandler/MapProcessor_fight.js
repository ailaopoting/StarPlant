/**
 * 战斗场景处理
 * @author
 *
 */
var MapProcessor_fight = (function (_super) {
    __extends(MapProcessor_fight, _super);
    function MapProcessor_fight(model) {
        _super.call(this, model);
    }
    var d = __define,c=MapProcessor_fight;p=c.prototype;
    return MapProcessor_fight;
})(MapProcessor);
egret.registerClass(MapProcessor_fight,"MapProcessor_fight");
